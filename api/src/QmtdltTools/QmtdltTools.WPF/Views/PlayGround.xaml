<Window x:Class="QmtdltTools.WPF.Views.PlayGround"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QmtdltTools.WPF.Views"
        mc:Ignorable="d"
        Title="PlayGround" Height="450" Width="800" WindowState="Maximized" PreviewKeyDown="Window_PreviewKeyDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200"/>
            <!-- Ensure video/subtitle area has minimum width -->
            <ColumnDefinition Width="8"/>
            <!-- Fixed width for GridSplitter -->
            <ColumnDefinition Width="300" MinWidth="250" />
            <!-- Constrain shadowing panel -->
        </Grid.ColumnDefinitions>
        
        <Grid HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="300"/>
            </Grid.RowDefinitions>
            <UserControl Content="{Binding VideoView}"/>
            <GridSplitter Grid.Row="1" VerticalAlignment="Stretch" Background="Gray" HorizontalAlignment="Stretch" Margin="1">
                <GridSplitter.Template>
                    <ControlTemplate TargetType="GridSplitter">
                        <Border Background="{TemplateBinding Background}" CornerRadius="1.5">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse Width="4" Height="4" Fill="White" Margin="4,0,4,0"/>
                                <Ellipse Width="4" Height="4" Fill="White" Margin="4,0,4,0"/>
                                <Ellipse Width="4" Height="4" Fill="White" Margin="4,0,4,0"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </GridSplitter.Template>
            </GridSplitter>
            <Grid Grid.Row="2" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBox TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"  FontSize="28" FontWeight="Light" BorderThickness="0" Text="{Binding PastSubtitle}"  
                         MouseDoubleClick="TextBox_MouseDoubleClick"
                        PreviewMouseUp="TextBox_PreviewMouseUp"></TextBox>
                <TextBox TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Top" FontSize="28" FontWeight="Bold" BorderThickness="0" Text="{Binding CurSubtitle}" 
                         Grid.Row="1" 
                         MouseDoubleClick="TextBox_MouseDoubleClick"
                         PreviewMouseUp="TextBox_PreviewMouseUp"></TextBox>
            </Grid>
        </Grid>

        <!--<GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Background="#000000" HorizontalAlignment="Stretch"/>-->
        <GridSplitter Grid.Column="1" VerticalAlignment="Stretch" Background="Gray" HorizontalAlignment="Stretch" Margin="1">
            <GridSplitter.Template>
                <ControlTemplate TargetType="GridSplitter">
                    <Border Background="{TemplateBinding Background}" CornerRadius="1.5">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Ellipse Width="4" Height="4" Fill="White" Margin="0,4,0,4"/>
                            <Ellipse Width="4" Height="4" Fill="White" Margin="0,4,0,4"/>
                            <Ellipse Width="4" Height="4" Fill="White" Margin="0,4,0,4"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </GridSplitter.Template>
        </GridSplitter>
        
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Command="{Binding AudioRecordCmd}" Content="{Binding RecordBtnContent}" />
                <Button Command="{Binding CheckShadowingCmd}" IsEnabled="{Binding IsSubmitEnable}" Grid.Column="1">提交录音</Button>
            </Grid>
            <MediaElement Grid.Row="1" Source="{Binding RecordAudioUri}" Visibility="{Binding AudioPlayerVisibility}"  Margin="0,10,0,0" LoadedBehavior="Manual" />
            <TextBlock Grid.Row="2" Text="{Binding StatusText}" ></TextBlock>
            <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14">发音分数</TextBlock>
                    <TextBlock FontSize="16" Text="{Binding PronunciationResult.PronunciationScore}"></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14">发音准确度</TextBlock>
                    <TextBlock FontSize="16" Text="{Binding PronunciationResult.AccuracyScore}"></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14">语音的流畅度</TextBlock>
                    <TextBlock FontSize="16" Text="{Binding PronunciationResult.FluencyScore}"></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14">完整性</TextBlock>
                    <TextBlock FontSize="16" Text="{Binding PronunciationResult.CompletenessScore}"></TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock FontSize="14">韵律</TextBlock>
                    <TextBlock FontSize="16" Text="{Binding PronunciationResult.ProsodyScore}"></TextBlock>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>